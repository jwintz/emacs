--- a/src/nsterm.m
+++ b/src/nsterm.m
@@ -7849,21 +7849,8 @@
     else if (fs_state == FULLSCREEN_NONE && ! maximizing_resize
              && [[self window] title] != NULL)
       {
-        char *size_title;
-        NSWindow *window = [self window];
-        if (old_title == 0)
-          {
-            char *t = strdup ([[[self window] title] UTF8String]);
-            char *pos = strstr (t, "  —  ");
-            if (pos)
-              *pos = '\0';
-            old_title = t;
-          }
-        size_title = xmalloc (strlen (old_title) + 40);
-	esprintf (size_title, "%s  —  (%d × %d)", old_title, cols, rows);
-        [window setTitle: [NSString stringWithUTF8String: size_title]];
-        [window display];
-        xfree (size_title);
+        /* Liquid Glass: Don't modify window title during resize.
+           SwiftUI NavigationSplitView manages the toolbar/title area. */
       }
   }
 #endif /* NS_IMPL_COCOA */
@@ -8772,6 +8759,9 @@
 
 - (instancetype)toggleToolbar: (id)sender
 {
+  /* Liquid Glass: Toolbar is managed by SwiftUI NavigationSplitView */
+  return self;
+
   NSTRACE ("[EmacsView toggleToolbar:]");
 
   if (!emacs_event)
@@ -9364,6 +9354,10 @@
 
 - (void)createToolbar: (struct frame *)f
 {
+  /* Liquid Glass: Don't create Emacs NSToolbar.
+     SwiftUI NavigationSplitView provides the toolbar. */
+  return;
+
   if (FRAME_UNDECORATED (f) || !FRAME_EXTERNAL_TOOL_BAR (f) || [self toolbar] != nil)
     return;
 
