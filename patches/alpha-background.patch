diff --git a/src/macfont.m b/src/macfont.m
--- a/src/macfont.m
+++ b/src/macfont.m
@@ -2942,9 +2942,18 @@ macfont_draw (struct glyph_string *s, int from, int to, int x, int y,

   block_input ();

-  if (with_background)
-    background_rect = CGRectMake (x, y - FONT_BASE (s->font),
-                                  s->width, FONT_HEIGHT (s->font));
+  /* Liquid Glass: Only draw background if it differs from the default face.
+     This allows the SwiftUI vibrancy layer to show through for text on the
+     default background while preserving syntax highlighting backgrounds. */
+  if (with_background)
+    {
+      struct face *default_face = FRAME_DEFAULT_FACE (f);
+      if (default_face && face->background == default_face->background)
+        background_rect = CGRectNull;  /* Transparent - let vibrancy show */
+      else
+        background_rect = CGRectMake (x, y - FONT_BASE (s->font),
+                                      s->width, FONT_HEIGHT (s->font));
+    }
   else
     background_rect = CGRectNull;

diff --git a/src/nsterm.m b/src/nsterm.m
--- a/src/nsterm.m
+++ b/src/nsterm.m
@@ -2628,8 +2628,8 @@ ns_clear_frame (struct frame *f)

   block_input ();
   ns_focus (f, &r, 1);
-  [[NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND
-			    (FACE_FROM_ID (f, DEFAULT_FACE_ID))] set];
+  /* Liquid Glass: Frame background is always transparent */
+  [[NSColor clearColor] set];
   NSRectFill (r);
   ns_unfocus (f);

@@ -2657,7 +2657,14 @@ ns_clear_frame_area (struct frame *f, int x, int y, int width, int height)

   r = NSIntersectionRect (r, [view frame]);
   ns_focus (f, &r, 1);
-  [[NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)] set];
+  {
+    /* Liquid Glass: default face background = transparent, others = opaque */
+    struct face *default_face = FRAME_DEFAULT_FACE (f);
+    if (default_face && face->background == default_face->background)
+      [[NSColor clearColor] set];
+    else
+      [[NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)] set];
+  }

   NSRectFill (r);

@@ -2760,7 +2767,14 @@ ns_clear_under_internal_border (struct frame *f)
         return;

       ns_focus (f, NULL, 1);
-      [[NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)] set];
+      {
+        /* Liquid Glass: internal borders use clearColor for default bg */
+        struct face *default_face = FRAME_DEFAULT_FACE (f);
+        if (default_face && face->background == default_face->background)
+          [[NSColor clearColor] set];
+        else
+          [[NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)] set];
+      }
       NSRectFill (NSMakeRect (0, margin, width, border));
       NSRectFill (NSMakeRect (0, 0, border, height));
       NSRectFill (NSMakeRect (0, margin, width, border));
@@ -2812,7 +2826,14 @@ ns_after_update_window_line (struct window *w, struct glyph_row *desired_row)
           NSRect r = NSMakeRect (0, y, FRAME_PIXEL_WIDTH (f), height);
           ns_focus (f, &r, 1);

-          [[NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)] set];
+          {
+            /* Liquid Glass: internal border areas use clearColor */
+            struct face *default_face = FRAME_DEFAULT_FACE (f);
+            if (default_face && face->background == default_face->background)
+              [[NSColor clearColor] set];
+            else
+              [[NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)] set];
+          }
           NSRectFill (NSMakeRect (0, y, width, height));
           NSRectFill (NSMakeRect (FRAME_PIXEL_WIDTH (f) - width,
                                   y, width, height));
@@ -2978,7 +2999,14 @@ ns_draw_fringe_bitmap (struct window *w, struct glyph_row *row,
     {
       NSTRACE_RECT ("clearRect", clearRect);

-      [[NSColor colorWithUnsignedLong:face->background] set];
+      {
+        /* Liquid Glass: fringe background = transparent for default face */
+        struct face *default_face = FRAME_DEFAULT_FACE (f);
+        if (default_face && face->background == default_face->background)
+          [[NSColor clearColor] set];
+        else
+          [[NSColor colorWithUnsignedLong:face->background] set];
+      }
       NSRectFill (clearRect);
     }

@@ -4143,7 +4171,14 @@ ns_draw_stretch_glyph_string (struct glyph_string *s)
 	      [[NSColor colorWithUnsignedLong: face->background] set];
 	    }
 	  else
-	    [[NSColor colorWithUnsignedLong: s->face->background] set];
+	    {
+	      /* Liquid Glass: stretch glyph bg = transparent for default */
+	      struct face *default_face = FRAME_DEFAULT_FACE (s->f);
+	      if (default_face && s->face->background == default_face->background)
+	        [[NSColor clearColor] set];
+	      else
+	        [[NSColor colorWithUnsignedLong: s->face->background] set];
+	    }
 	  NSRectFill (NSMakeRect (x, y, w, h));
 	}
     }
@@ -4174,7 +4209,14 @@ ns_draw_stretch_glyph_string (struct glyph_string *s)
 	  if (s->stippled_p)
 	    [[dpyinfo->bitmaps[s->face->stipple - 1].img stippleMask] set];
 	  else
-	    [[NSColor colorWithUnsignedLong: s->face->background] set];
+	    {
+	      /* Liquid Glass: stretch glyph background = transparent */
+	      struct face *default_face = FRAME_DEFAULT_FACE (s->f);
+	      if (default_face && s->face->background == default_face->background)
+	        [[NSColor clearColor] set];
+	      else
+	        [[NSColor colorWithUnsignedLong: s->face->background] set];
+	    }

 	  NSRectFill (NSMakeRect (x, s->y, background_width, s->height));
 	}
@@ -8605,7 +8647,7 @@ - (void)toggleFullScreen: (id)sender

       [w setContentView:[fw contentView]];
       [w setBackgroundColor: col];
-      if ([col alphaComponent] != (EmacsCGFloat) 1.0)
+      if (true)  /* Liquid Glass: window is always non-opaque */
         [w setOpaque: NO];

       f->border_width = [w borderWidth];
@@ -9343,8 +9385,9 @@ - (instancetype) initWithEmacsFrame: (struct frame *) f

       col = [NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND
                                      (FACE_FROM_ID (f, DEFAULT_FACE_ID))];
-      [self setBackgroundColor:col];
-      if ([col alphaComponent] != (EmacsCGFloat) 1.0)
+      /* Liquid Glass: window background is always transparent */
+      [self setBackgroundColor: [NSColor clearColor]];
+      if (true)  /* Liquid Glass: window is always non-opaque */
         [self setOpaque:NO];

       /* toolbar support */
